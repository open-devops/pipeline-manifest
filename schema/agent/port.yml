---
id: port.json
"$schema": "http://json-schema.org/draft-04/schema"
title: Port definition
description: ""
type:
- object
properties:
  name:
    name: name
    type: string
    description: Port name
    example: build
  description:
    name: description
    type: string
    description: Longer textual description of the port
    example: Fetch source code and build with maven
  type:
    name: type
    type: string
    description: Port datatype
    example: operation
  required:
    name: required
    type: boolean
    description: Whether the port needs to be connected for the component to work
    example: false
required: [name,type]
